#!/usr/bin/node
const x = parseint(process.argv[2]);
const ch = 'X';

if (isNaN(x){
	console.log('Missing size');
}else{
	for ( let i = 0; i < x; i++){
		for ( let j = 0; j < i; j++){
			console.log(ch);
		}
	}
}
