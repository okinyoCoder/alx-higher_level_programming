#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new_matrix = []
    for i in range(len(new_matrix)):

        for j in range(len(new_matrix[0])):
            new_matrix[i][j**2]
    return new_matrix
